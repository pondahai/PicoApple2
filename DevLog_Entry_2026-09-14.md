# 🍎 PicoApple2 DevLog - 2026-09-14

## 🖥️ 底部說明列：新增 F Key 速查、改為按鍵喚出 3 秒後自動隱藏（實機驗證通過 ✅）

### 1. 背景：狀態列只講「現在是什麼」，不講「能按什麼」
原本螢幕底部只有一列狀態（`ARROWS:` / `SPEED:` / `COLOR|GREEN`），固定顯示在 y=222。
面板上印著 Fn+數字，但機器上沒有任何地方說明 F1~F7 各自做什麼，等於得背。

模擬畫面只佔 y=0..191（`tft_dma.startFrame(20, y, 299, y)`），y=192..239 那 48px 本來就是空的，
容得下第二列，不必動到模擬畫面的任何時序。

### 2. 版面：狀態列抬高，下面插一列 F Key 速查
```
y=210  ARROWS: KEYBOARD   SPEED: X1.0   COLOR
y=222  F1:WRST F2:CRST F3:DISK F4:JOY F5:SPD F7:GRN
```
- 字型固定 7px 寬，一列從 x=6 起算**最多 44 字**；上面那串剛好 44 字填滿，所以：
  - 不放 `FN+` 前綴（會吃掉 4 字，得砍掉一項）。
  - **不列 CapsLock** —— 鍵盤上有實體 CAPS 鍵（偽碼 208，`k=0` 不送模擬器）可直接按，
    列出 `Fn+C` 反而讓人以為單按沒用。`Fn+C` 那條重複路徑保留不動。
- 鍵名綠、功能縮寫白，沿用狀態列「標籤綠、內容亮」的既有語彙。
- 內容由 `FKEY_HINTS[]` 表驅動（欄位存**字元索引**不是像素），要增刪改表即可。

### 3. 行為：平時隱藏，按 Fn / F 鍵亮 3 秒
遊戲時螢幕下方保持全黑，不佔視覺。觸發點有二：
1. **按下實體 Fn 鍵的瞬間**（`scan_matrix()` 裡 `r==3 && c==7` 的按下邊緣）。
   實機體感是「按住 Fn 看一眼再按數字」，後面組出的 F 鍵只是把倒數蓋掉重算。
2. **任一 F 鍵動作**（`g_f_key_event` 分派的最前面）。外接鍵盤/網頁沒有 Fn 鍵，
   靠這條現身 —— 等於按下去才看到，順便確認切換結果。

實作上把 `updateStatusLine()` **沿用原名改成「觸發」語意**（亮起＋倒數歸零＋重畫），
實際繪製拆到 `paintStatusLines()`。好處是原本每個「狀態變動後重畫」的呼叫點
（F4/F5 切換、Fn+7、離開選單後的重畫）**自動全部變成觸發點**，不必逐一改。

### 4. ⚠️ 自動隱藏的兩個坑
- **必須掛在 Core 1 的 VBLANK**：`statusLineTick()` 緊接在 `scan_matrix()` 之後，
  此刻 SPI 匯流排是靜的。從 Core 0 插 SPI 會撞到掃描線 DMA。
- **清除不能用 `fillScreen`**（一次約 100ms，會直接掉幀）。只擦兩列字高的
  `drawRect(0, 208, 320, 24, 0)`，約 2ms，塞得進 VBLANK。
- **選單開啟時不擦**：選單畫面自己佔滿整頁，**下緣外框就在 y=228**，
  擦了會在外框上打一個洞。`statusLineTick()` 開頭 `if (g_show_menu) { g_status_visible = false; return; }`
  —— 選單的 `fillScreen` 已經清乾淨，只要清旗標即可。
- 馬達燈在 `(305, 196, 8, 8)`，在 208 之上，不受清除範圍影響。

### 5. 順手補上：網頁終端機的 F7
網頁端原本只做到 F5，F7 從缺 —— 因為**內部 Fn+7 是直接翻 `g_mono_green` 旗標，
不走 `g_f_key_event` 信箱**，所以序列埠那條路根本沒有入口。

補成一整條通路：
- 韌體新增 `g_f_key_event == 7`，入口是 `ESC [18~`（真正的 ANSI F7 序列，原本沒用到）。
- `Apple2Keyboard.html` 的 F7 送這串，說明面板加一行 `F7 Color/Green`。
- ⚠️ 分派開頭那次 `updateStatusLine()` 畫的是**舊值**，所以 7 的分支翻完旗標要**再畫一次**
  （F4/F5 本來就是這樣處理）。

⚠️ **兩端都要更新**：舊韌體不認得 `[18~`，收到會整串丟掉、完全無反應。
只改網頁不燒韌體的症狀就是「按 F7 沒事發生，但 F1/F2/F3 正常」。

### 6. 📌 順手修掉的兩項 F 鍵映射落差（同日，實機待驗）

**(a) F 鍵差一格。** 原本 `[15~`（實體 **F5**）→ 事件 4、`[17~`（實體 **F6**）→ 5。
`Apple2Keyboard.html` 是照這個錯位寫的（按 F4 送 `[15~`），兩邊一起錯所以網頁端一直正確；
只有第三方序列終端（PuTTY 等）直連才會露餡：按 F4 送標準的 `[14~`，韌體表裡沒有 → 整串丟掉、
沒反應；F5 變成切搖桿、F6 變成切速度，跟畫面上的 `F4:JOY F5:SPD` 完全對不起來。

改成正規映射，**兩端一起動**：

| 實體按鍵 | 標準序列 | 事件 | 功能 |
| :--- | :--- | :--- | :--- |
| F1 | `[11~` / `OP` | 1 | Warm Reset |
| F2 | `[12~` / `OQ` | 2 | Cold Reset |
| F3 | `[13~` / `OR` | 3 | Disk Menu |
| F4 | `[14~` / `OS` | 4 | Joy/Key |
| F5 | `[15~` | 5 | Speed |
| F6 | `[17~` | — | 不接（速查列上也沒有 F6） |
| F7 | `[18~` | 7 | Color/Green |

順帶挖出一個**從來沒生效過的死碼**：`strcmp(esc_buf, "OP")` / `"OS"` 那幾個比對寫在
`esc_buf[0] == '['` 的分支**裡面** —— 進得去那個分支就表示第一個字元是 `'['`，不可能等於 `'O'`。
真正在處理 SS3 的是下面的 `else if (esc_buf[0] == 'O')` 分支，而它原本只認 P/Q/R，
所以 `ESC O S`（VT100 式的 F4）一直是沒人接的。現已補上 S → 事件 4。

**(b) `'K'` 封包漏字元。** 網頁協定的 `'K'`（即時打字）路徑原本只認 idx 112/113/114
（F1~F3 的 JS keyCode），其餘一律 `pushKey(idx)`。JS keyCode 115 是 F4，而 ASCII 115 是 `'s'`
—— 客戶端只要送 F4，Apple II 畫面上就憑空多一個 `s`。自家網頁因為 F4 走的是 (a) 那條
ESC 序列，碰不到；但這是留給別人踩的地雷。

改成 **112~123（F1~F12）整段吃掉**：112~116 → 事件 1~5、118 → 事件 7，
F6 與 F8~F12 無對應功能也一併吞掉不送。這樣往後在這條路加鍵，預設就不會漏字元。
